---
kind: Secret
apiVersion: v1
metadata:
  name: json-key-password
type: Opaque
data:
  {{- if .Values.global.isPrimaryCluster }}
  json-key-password: "{{ .Values.global.primaryJsonWalletPassword | b64enc }}"
  {{- else }}
  json-key-password: "{{ .Values.global.secondaryJsonWalletPassword | b64enc }}"
  {{- end }}
