---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: "{{ .Chart.Name }}-thanos-ingress"
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-cluster-issuer"
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  tls:
    - hosts:
        - "{{ .Values.global.region }}.thanos.{{ .Values.global.domain }}"
      secretName: "{{ .Chart.Name }}-thanos-ingress-tls-secret"
  ingressClassName: nginx
  rules:
    - host: "{{ .Values.global.region }}.thanos.{{ .Values.global.domain }}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: "{{ .Chart.Name }}-kube-prometheus-sta-thanos-discovery"
                port:
                  number: 10901
