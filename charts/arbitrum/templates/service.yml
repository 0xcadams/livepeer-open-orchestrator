---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  labels:
    app: {{ .Chart.Name }}
spec:
  ports:
    - port: 8547
      targetPort: rpc
    - port: 8548
      targetPort: ws
    - port: 9642
      targetPort: seq
  selector:
    app: {{ .Chart.Name }}
  type: ClusterIP
