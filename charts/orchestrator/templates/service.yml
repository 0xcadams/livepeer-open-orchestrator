---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  labels:
    app: {{ .Chart.Name }}
spec:
  ports:
    - port: 8935
      targetPort: https
  selector:
    app: {{ .Chart.Name }}
  type: ClusterIP
